
export enum ColumnTypes {
    THREE_COLUMN = "three-column",
    ONE_COLUMN = "one-column",
    TWO_COLUMN = "two-column",
  }
  
  export enum VariantTypes {
    FEATURED_ROOM = "featured-room",
    ALL_ROOM = "all",
  }
  